{
  "name": "icon-set",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "check:web": "npm --workspace apps/web run check",
    "dev:api": "cargo run -p icon-set-api",
    "check:api": "cargo check -p icon-set-api"
  }
}
